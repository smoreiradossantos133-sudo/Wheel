// Math helpers for Wheel standard library (skeleton)

/// Returns an approximation of the square root (placeholder)
pub fn sqrt_f64(x: f64) -> f64 {
    x.sqrt()
}

/// Example: add two integers
pub fn add_i64(a: i64, b: i64) -> i64 {
    a + b
}
